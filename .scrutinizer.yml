checks:
    php:
        code_rating: true
        duplication: true
    tests:
        override:
            -
                command: 'phpunit --coverage-clover=some-file'
                coverage:
                    file: 'some-file'
                    format: 'php-clover'
filter: {  }
coding_style:
    php:
        indentation:
            switch:
                indent_case: false
build: true

